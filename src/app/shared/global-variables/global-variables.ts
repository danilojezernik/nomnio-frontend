/**
 * Enum defining keys for storing weather data in local storage.
 * These keys are used to save and retrieve weather data locally.
 */
export enum LOCAL_STORAGE_KEYS {
  WEATHER_DATA_CURRENT = 'weatherDataCurrent', // Key for storing current weather data
  WEATHER_DATA_FORECAST = 'weatherDataForecast' // Key for storing weather forecast data
}
